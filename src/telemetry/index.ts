/**
 * Telemetry barrel — import everything from one place:
 *   import { recordRejection, recordExecution, generateEntryReport, logAnalyticsSummary } from '../telemetry';
 */
export * from './types';
export * from './signalTelemetry';
